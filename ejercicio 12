#Escribir un programa que pregunte el nombre del usuario en la consola y 
#un número entero e imprima por pantalla en líneas distintas el nombre del usuario tantas 
#veces como el número introducido.

nombre = input("ingrese su nombre ")
numero = int(input("ingrese el numero de veces que quiera que se repita "))

for numero in range (0 , numero):
    print(nombre)
